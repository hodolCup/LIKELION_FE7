프로토타입.. 중요하진 않은데 면접에 자주나와.

prototype은 항상 함수안에 존재
__proto__는 객체에 있다.
둘은 서로를 참조하고있다.

같은 값을 참조하게 한다 + 상속이 가능하다 === 프로토타입으로 등록한다


프로토타입 체이닝 --> 면접에 굉장히 많이나옴.

함수도 객체. 고로 무언가의 인스턴스

autoBoxing..? 
원시타입도 매서드를 쓸수 잇게하는


class 문법.

super 주의점?

아래 써서 덮어씌우기.
오버라이딩

클래스 문법.

17. 최적화 파트