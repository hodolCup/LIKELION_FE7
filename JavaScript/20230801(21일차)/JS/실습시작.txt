외부 스크립트(002.html)을 잘 안쓰는 이유?
유지보수가 어려워서. 마크업까지 해야하는데..
문제가 생기면 003의 경우 003.js만 보면 됨.

001~3을 해봤다.

BOM은 JS가 아님. 브라우저에서 제공하는 API

컨트롤 시프트 i : 개발창
콘솔창에 명령어를 쳤을때 해당 명령어 종류를 볼수있음.
ex)window 입력후 화살표


변수. 
변수는 세가지 가능.
var x = 3;
let y =4;
const z = 5;

var는 전역 변수를 선언할 때 사용합니다. 이제는 사용하시면 안됩니다. 
함수스코프를 갖는다라고 얘기합니다.

const를 사용하세요! 보통은 '변하지 않는 값을 선언할 때 사용합니다.'라고 얘기하는데 
저는 const로 사용해서 문제가 없으면 const를 사용하세요. 
블록 안에(중괄호 안에) 선언이 되었을 경우 밖에서 
이 값을 사용할 수 없습니다. 블록 스코프를 갖는다라고 얘기합니다.

let은 const로 사용해서 문제가 발생하는 경우 사용하세요. 
보통은 '변하는 값을 선언할 때 사용합니다.' 블록 안에(중괄호 안에) 
선언이 되었을 경우 밖에서 이 값을 사용할 수 없습니다. 
블록 스코프를 갖는다라고 얘기합니다.

010 5044 2903

004.5 콘솔, 디버깅
006. 닌자코드..
007. 지역변수, 전역변수.. 스코프. 범위
009. typeof는 정확하지않다. typecheck이 정확
10 원시타입과 특징. 일단 string만 했다. 
11~13. 문자열과 사직연산
11. string
${} : 변수
parseInt 안정성이 높다. 실무에서 자주사용

13. 자주 쓰는부분.

const indexOfC = Str2.indexOf('C');
console.log(str2.slice(indexOfC, 20));

문제 예문 복습하도록..


